export class User {
    constructor(id, firstName, lastName, occupation, gender) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.occupation = occupation;
        this.gender = gender;
    }
}